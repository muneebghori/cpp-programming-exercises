#include <vector>

void display_misses(int misses);

// Declare functions
void greet();
std::string end_game(std::string answer, std::string codeword);
void display_status(std::vector<char> incorrect, std::string answer);